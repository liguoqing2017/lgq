server:
  port: 8080

spring:
  application:
    name: ai-project

# Actuator 配置
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
